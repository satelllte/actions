name: Self-hosted

on:
  workflow_dispatch:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  self_hosted:
    runs-on: self-hosted
    steps:
      - name: Test log
        shell: powershell
        run: Write-Output "Hi from self-hosted runner!"
